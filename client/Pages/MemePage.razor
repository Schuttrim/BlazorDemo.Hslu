@page "/meme"
<h1>Calling the Meme Service</h1>
<button class="btn btn-primary" @onclick="FetchMeme" >One does not simply</button>

@if (!string.IsNullOrEmpty(Answer))
{
    <p>Answer: @Answer</p>
    <button class="btn btn-primary" @onclick="Reset" >Reset</button>
}
